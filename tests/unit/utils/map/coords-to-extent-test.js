import mapCoordsToExtent from 'audacious-app/utils/map/coords-to-extent';
import { module, test } from 'qunit';

module('Unit | Utility | map/coords-to-extent', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = mapCoordsToExtent();
    assert.ok(result);
  });
});
